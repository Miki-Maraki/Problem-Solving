package polymorsim;

public class HowToUseSuper {
/*
	public class Mannager extends Employee {
		private double bonus;

		// Rules for Subclass Constructors: ---->> a subclass constructor must make use of one
		// of the constructors from its superclass
		public Mannager(String name, double salary, int year, int month, int day) { // p.constructor
			super(name, salary, year, month, day);
			bonus = 0;
		}

		@Override
		public double getSalary() {
			// bc we can not direct access to private variables of superclass
			 
??????			double baseSalary = super.getSalary();  // ............ ????
			    return baseSalary + bonus;
		}

		public void setBonus(double b) {
			this.bonus = b;
		}
	}

*/
}
